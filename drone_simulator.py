"""
drone_simulator.py — NO LONGER NEEDED

The UTM system no longer uses a pre-registered drone fleet.
Drones are spawned on-demand when a delivery is requested (main.py)
and removed automatically when the mission ends.

You do NOT need to run this file. Just run:

    python main.py

Then open http://localhost:8000 and assign missions from the UI.
"""

print("drone_simulator.py is no longer required.")
print("Run only: python main.py")